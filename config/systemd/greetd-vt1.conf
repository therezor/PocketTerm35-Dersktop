# Debian's greetd unit conflicts with getty@tty7, but pt35-desktop runs on VT1.
# Without this, stopping greetd lets getty@tty1 take the VT, and the next start
# of greetd finds it busy: the panel stays black until a reboot.
[Unit]
Conflicts=getty@tty1.service
After=getty@tty1.service

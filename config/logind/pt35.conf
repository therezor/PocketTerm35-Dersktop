# A short press of the power button must not kill a handheld that lives in a
# bag. logind ignores it, sway binds XF86PowerOff to the power menu, and a long
# press still cuts power as the escape hatch.
[Login]
HandlePowerKey=ignore
HandlePowerKeyLongPress=poweroff
HandleLidSwitch=ignore
